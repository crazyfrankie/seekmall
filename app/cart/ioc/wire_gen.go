// Code generated by Wire. DO NOT EDIT.

//go:generate go run -mod=mod github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package ioc

import (
	"fmt"
	"github.com/crazyfrankie/seekmall/app/cart/biz/repository"
	"github.com/crazyfrankie/seekmall/app/cart/biz/repository/dao"
	"github.com/crazyfrankie/seekmall/app/cart/biz/service"
	"github.com/crazyfrankie/seekmall/app/cart/config"
	"github.com/crazyfrankie/seekmall/app/cart/rpc"
	"go.etcd.io/etcd/client/v3"
	"gorm.io/driver/mysql"
	"gorm.io/gorm"
	"gorm.io/gorm/schema"
	"os"
	"time"
)

// Injectors from wire.go:

func InitServer() *rpc.Server {
	db := InitDB()
	cartDao := dao.NewCartDao(db)
	cartRepo := repository.NewCartRepo(cartDao)
	client := InitRegistry()
	productServiceClient := rpc.InitProductClient(client)
	cartServer := service.NewCartServer(cartRepo, productServiceClient)
	server := rpc.NewServer(cartServer, client)
	return server
}

// wire.go:

func InitDB() *gorm.DB {
	dsn := fmt.Sprintf(config.GetConf().MySQL.DSN, os.Getenv("MYSQL_USER"), os.Getenv("MYSQL_PASSWORD"), os.Getenv("MYSQL_HOST"), os.Getenv("MYSQL_PORT"), os.Getenv("MYSQL_DB"))

	db, err := gorm.Open(mysql.Open(dsn), &gorm.Config{
		NamingStrategy: &schema.NamingStrategy{
			SingularTable: true,
		},
	})
	if err != nil {
		panic(err)
	}

	db.AutoMigrate(&dao.Item{})

	return db
}

func InitRegistry() *clientv3.Client {
	cli, err := clientv3.New(clientv3.Config{
		Endpoints:   []string{config.GetConf().ETCD.Addr},
		DialTimeout: 5 * time.Second,
	})
	if err != nil {
		panic(err)
	}

	return cli
}
